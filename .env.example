# ─── Strava ────────────────────────────────────────────────────────────────
# Create an API app at https://www.strava.com/settings/api
# then run:  python scripts/strava_auth.py
STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret
STRAVA_REFRESH_TOKEN=your_refresh_token   # obtained via strava_auth.py

# ─── Garmin Connect ─────────────────────────────────────────────────────────
GARMIN_EMAIL=your@email.com
GARMIN_PASSWORD=your_password

# Your watch's Unit ID (NOT the serial number on the box).
# Find it in Garmin Connect app → Devices → [Your Device] → System → About
GARMIN_DEVICE_UNIT_ID=1234567890

# Garmin product ID for your watch model (controls Training Effect calculation).
# Common values:
#   3290 = Forerunner 965
#   3143 = Forerunner 955
#   3196 = Fenix 7
#   3530 = Fenix 8
# Full list: https://developer.garmin.com/fit/protocol/ (search "GarminProduct")
GARMIN_PRODUCT_ID=3290

# ─── Optional tuning ────────────────────────────────────────────────────────
# How far back to look on the very first run (seconds). Default: 6 hours.
LOOKBACK_SECONDS=21600

# Path to the state file that tracks already-synced activity IDs.
# Default: ~/.spin-sync-state.json
# STATE_FILE=/custom/path/to/state.json
